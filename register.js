var rootRef = firebase.database().ref().child(_Events/event_users); 

function appendChildren(_Events, event_users){
    event_users.forEach(event_users){
        _Events.appendChild(event_users); 
    }
}
$('#register_button').click(function(){
        sessionStorage.getItem(['firstname']); 
        sessionStorage.getItem(['lastname']); 
    });
}